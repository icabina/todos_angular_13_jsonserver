App ToDo,
Jorge Roldán, 2022.

==========================================
npm i -g @angular/cli
npm i -g json-server
==========================================
1. Instalar proyecto (npm i)
2. Ejecutar json-server (json-server --watch db.json)
3. ng serve
4. ng test

==========================================
ESTRUCTURA:

El proyecto se implemento usando UI de angular material,
(Dialog, Table, Input, Icon, Toolbar, etc, etc...)

* app.component (Componente principal)

* dialog.component (Componente hijo)

* api.service (Servicio para llamadas http a JSON-SERVER)

